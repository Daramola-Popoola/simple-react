import React, { useReducer } from 'react';
import './App.css';
import CounterOne from './components/CounterOne'

const initialScale = 0;
const reducer  = (state, action) => {
  return newState = 
}

function App() {
  useReducer(reducer, initialScale)
  return (
    <div className="App">
        <CounterOne />
        hello world
    </div>
  );
}

export default App;
